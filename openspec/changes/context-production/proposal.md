# Change: Context Production

## Objective
Make context a deliberately produced artifact, not an accidental side effect. The agent must decide what context to emit, in what shape, with what freshness guarantees, and where to store it.