# Change: Completion Integrity

## Objective
Make task completion a verifiable claim. Every "done" status MUST be backed by machine-checkable evidence (tests, lints, typechecks, file existence, schema validation) so the agent cannot declare success on insufficient grounds.