# Change: Runtime Hardening

## Objective
Harden the agent's runtime against unsafe inputs, hostile environments, resource exhaustion, and unintended side effects. The hardened runtime enforces invariants continuously, not only at tool-call boundaries.