# Change: Durable State

## Objective
Persist agent state (assessments, plans, evidence, completion reports, drift logs) so that work survives crashes, restarts, and session boundaries. State is durable, queryable, and recoverable.